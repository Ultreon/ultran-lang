program Main;
var x : integer; startX : integer; startY : integer;

function Alpha(a : integer; b : integer);
    var x : integer;

    function Beta(a : integer; b : integer);
    var x : integer;
    {
        x := a * 10 + b * 2;
    };
{
    x := (a + b ) * 2;
    Beta(5, 10);      [ function call ]
};

{  [ Main ]
    Alpha(3 + 5, 7);  [ function call ]
    x := 300;
    startX := 10;
    startY := 20;
    print("Hello World! Number: " + randInt(startX, startY));
}. [ Main ]
